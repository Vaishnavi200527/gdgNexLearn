# Fix is Automated

The database seeding process has been automated in the `run_server.py` script. You no longer need to perform any manual steps.

Simply run the server to have the changes applied:

```bash
python hackathon-project/run_server.py
```
